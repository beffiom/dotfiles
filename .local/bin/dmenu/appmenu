#!/bin/sh
case "$(printf "productivity\\ninternet\\ngames\\nsettings" | dmenu)" in
	"productivity") productivity-menu ;;
	"internet") internet-menu ;;
	"games") games-menu ;;
	"settings") settings-menu ;;
esac
