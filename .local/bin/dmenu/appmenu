#!/bin/sh
case "$(printf "signal\\nlibreoffice\\nminetest\\npsp\\nvirt-manager" | dmenu)" in
	"signal") signal-desktop ;;
	"libreoffice") libreoffice ;;
	"minetest") minetest ;;
	"psp") ppsspp-sdl ;;
	"virt-manager") virt-manager ;;
esac
