#!/bin/sh
case "$(printf "mpv\\nfirefox\\ninvidio.us\\nlibreoffice\\nsignal" | dmenu)" in
	"mpv") mpv --player-operation-mode=pseudo-gui ;;
	"firefox") firefox ;;
	"invidio.us") qutebrowser :"open -w https://invidio.us" ;;
	"libreoffice") libreoffice ;;
	"signal") signal-desktop ;;
esac
